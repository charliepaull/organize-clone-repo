import ReactGA from 'react-ga'

const initial_state = {
}

const analytics = (state=initial_state, action) => {
	switch (action.type) {
		default:
			return state		
	}
}

export default analytics